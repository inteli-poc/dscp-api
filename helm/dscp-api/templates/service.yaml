apiVersion: v1
kind: Service
metadata:
  name: {{ include "dscp-api.fullname" . }}
  labels:
    {{- include "dscp-api.labels" . | nindent 4 }}
spec:
  selector:
    name: {{ include "dscp-api.fullname" . }}
  ports:
    - protocol: 'TCP'
      port: 80
      targetPort: {{ .Values.config.port }}